# Gemini Custom Rules

These rules are injected into .gemini/GEMINI.md during generation.

- Use shell tool for running Go commands.
- When working on tasks, use `/prd` slash command for PRD conversion.
- Check existing test coverage before adding new tests.
